- id: unnamed-ports
  title: Unnamed ports
  items:
    - example:
        operation: fixit
        layers:
          - code: |
              ports:
                - containerPort: 8080
            languages: [[0, "yaml"]]
          - code: |
              ports:
                api:
                  containerPort: 8080
                $encode: values:containerPort
            languages: [[0, "yaml"]]
          - code: |
              ports:
                api:
                  containerPort: 8080
                $encode: values
            languages: [[0, "yaml"]]